package network.propagators

import network.broadcast.BroadcastService
import transactions.Transaction

class TransactionPropagator: Propagator<Transaction> {
    override fun propagate(item: Transaction, peers: List<String>) {
        B
    }

}